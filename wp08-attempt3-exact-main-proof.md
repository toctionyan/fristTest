# WP-08 Attempt 3 exact-main proof

This temporary, unmerged proof branch starts exactly at main commit `83251fe9e5e20fe66f9f80c569583a9e189214c8`.

The only branch delta is this non-product proof file. `services/`, `web/`, and `contracts/` are byte-identical to that main commit. The PR exists only to make pull-request CI observable through the release tooling and must be closed unmerged after the gates complete.
