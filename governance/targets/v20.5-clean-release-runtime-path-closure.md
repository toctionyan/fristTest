# 目标

- 目标 ID：change-20260712-v20-5-clean-release-runtime-path-closure
- 变更标识：v20.5-clean-release-runtime-path-closure
- 执行上下文：local-change

修复 clean-release 将所有名为 `runtime` 的目录一律排除，导致生产 `agent_core/runtime` 源码和架构必需的 `tests/runtime` 未进入最终制品、包内架构自检失败的问题；让发布包在组成完整性之外同时具备可执行的 Skill、版本与架构自证。

## 允许范围

仅允许修改发布复制/验证脚本、对应反例测试、Skill 引用与发布边界文档、治理目标和重新生成的发布元数据。

- 允许变更路径：architecture-skill/**, scripts/**, services/agent-service/tests/**, governance/**, docs/**, release/**, README.md, CHANGELOG.md, Makefile
- 新增抽象记录：无

## 禁止范围

不修改业务事务主链、模型理解链、业务服务事实边界和数据库状态机；不得通过删除包内架构必需项、放宽 `verify_architecture.py` 或跳过最终解包自检恢复绿灯。

## 验收条件

- 最低质量模式：quick
- 服务根目录下的运行态 `services/*/runtime` 必须排除，但 `src/agent_core/runtime` 与 `tests/runtime` 必须进入 clean-release。
- clean-release 构建必须在打 ZIP 前对 staged tree 执行 Skill、版本和架构自检，任一失败即阻止构建。
- 最终 ZIP 解包后普通 `python` 完整性验证只读，Skill、版本和架构自检全部 PASS。
- 完整 quick required Gate 同轮 PASS，且新证据绑定修复后的源码快照。

## 基线

使用修复前 V20.5 工作区的 static baseline 冻结当前发布复制行为；随后以新增反例证明 runtime 同名源码/测试被错误排除，再修复唯一发布 Owner。

## 修复轮次

- 最大轮次：8
- 当前轮次：2
- 失败后：保留失败反例并推进下一轮；定向回归仅作中间证据，最终必须重新执行完整 quick。
