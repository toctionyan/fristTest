# 目标

- 目标 ID：change-20260712-v20-5-trustworthy-quality-release-runtime
- 变更标识：v20.5-trusted-quality-release-runtime
- 执行上下文：local-change

修复 Quality Loop 部分 Gate 假收敛与可编辑历史证据、Agent/Business profile 分裂和 verifier 降级、发布清单失真、会话租约过期并发写入、文档任务崩溃后永久卡死以及前端产物不进入源码/制品证明的问题。

## 允许范围

允许修改 Skill、治理策略、质量脚本、Agent/Business 源码与测试、迁移、配置模板、文档、发布元数据和 CI workflow。

- 允许变更路径：architecture-skill/**, services/agent-service/**, services/business-service/**, deployment/**, scripts/**, governance/**, docs/**, release/**, .github/workflows/**, Makefile, README.md, CHANGELOG.md, VERSION
- 新增抽象记录：docs/architecture/V20_5_TRUST_BOUNDARIES.md

## 禁止范围

不改变业务事务权威边界，不新增平行业务主链，不用删除或放宽既有反例恢复绿灯，不删除用户保留的 `.env`、数据库、依赖和开发 evidence。

## 验收条件

- 最低质量模式：quick
- 定向 Gate 只能得到 `TARGETED_REGRESSION_PASSED`，完整 required Gate 才能 `CONVERGED` 或 `CI_VERIFIED`。
- evidence 与源码快照、Gate 合同和证据文件哈希绑定，篡改或源码变化必须阻断。
- protected profile 禁止 verifier disabled/candidate，Agent/Business 统一 APP_PROFILE 并强制签名。
- 会话锁具有续租与 fencing；旧 Worker 无法持久化。
- 文档队列具有共享存储、lease/attempt/超时回收；旧任务 token 无法完成新 claim。
- release 文件集合与 SHA256 精确一致，clean artifact 重建前端、绑定完整 evidence 并解包复验。
- Skill、源码、测试、配置和发布文档无冲突。

## 基线

使用修改前 V20.4 工作区的 static baseline 记录源码身份；最终不以 static baseline 代替 quick 完成门槛。

## 修复轮次

- 最大轮次：8
- 当前轮次：2
- 失败后：每轮先增加或保留失败反例，再修改唯一 Owner；每轮只在真实代码、测试或合同变化后推进。停滞则重新规划，不原样重跑。
