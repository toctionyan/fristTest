# 目标

- 目标 ID：change-20260711-goal-coverage
- 变更标识：working-tree-v20.3-goal-coverage
- 执行上下文：local-change

让每个 user turn 在调用领域工具前声明并独立验证全部显式目标，使 Workflow 能阻止漏意图、相似能力替代和未映射工具后提前完成，并让 Skill、代码、测试和验证报告准确描述当前回合编排边界。

## 允许范围

- 允许变更路径：architecture-skill/**, services/agent-service/**, services/business-service/**, scripts/**, governance/**, docs/**, release/**, README.md, CHANGELOG.md, VERSION
- 新增抽象记录：docs/architecture/TURN_GOAL_PLAN_RECORD.md

## 禁止范围

不得改变 Business Service 的业务权威、Draft/Grant/Attempt/Receipt 主链、公开 HTTP/SSE 合同或把 soft task board 升格为业务/调度权威。不得将当前回合 Workflow 宣称为跨回合 Durable Workflow。

## 验收条件

- 最低质量模式：quick
- Runtime Contract Suite 与独立 Semantic Goal Oracle Suite 全部通过。
- 缺失声明目标、orphan Step、无独立覆盖证明时不能接受最终回答。
- Agent Service、Business Service 标准测试零失败零 skipped；前端 Vitest 与 build 通过。
- development-workspace profile 保留运行资产，不以清洁发布 Gate 阻断本轮架构验收。

## 基线

使用当前 V20.3 Goal Coverage 候选工作区运行 `--baseline`，后续仅允许根据 quick Gate 的 repair-plan 做最终对齐修复。前置手工 Loop 已记录在 `release/VALIDATION_REPORT.md`。

## 修复轮次

- 最大轮次：8
- 当前轮次：3
- 失败后：根据 repair-plan.json 的 Owner 与 rerun 命令做最小修复；失败后将当前轮次加一。超过最大轮次即停止并交付 evidence，不再扩大范围。
