# 目标

- 目标 ID：change-20260712-environment-block-propagation-v206
- 变更标识：working-tree-v20.6-environment-block-propagation
- 执行上下文：local-change
- 目标类型：certification

修复 claim 状态传播：环境 Gate 阻断时，依赖它而未执行的 release claim 必须保持未认证/环境阻断，不得误报为代码失败，也不得获得完成资格。

## 允许范围

允许修改质量控制器、证据 schema、Skill 质量合同、架构文档和对应反例。

- 允许变更路径：scripts/quality_loop.py, governance/**, architecture-skill/**, docs/**, services/agent-service/tests/architecture/test_quality_loop_controller.py, VERSION, README.md, CHANGELOG.md, release/**
- 新增抽象记录：无

## 禁止范围

不把真实 Gate FAIL 降级成环境阻断；不让 blocked claim 变成 VERIFIED；不复用上一目标的 PASS 证据。

## 验收条件

- 最低质量模式：quick
- 声明清单：governance/claims/v20.6-environment-block-propagation.json
- 验收 ID：QL-ENV-BLOCK-001

反例必须证明纯环境阻断沿依赖链传播为 `BLOCKED_BY_ENVIRONMENT`；混合真实 FAIL 的依赖链仍必须为 `FAILED`。

## 基线

以同一 target、claim 和 policy 的修复前控制器建立独立 baseline 和失败轮。

## 修复轮次

- 最大轮次：8
- 当前轮次：2
- 失败后：保留失败证据，修复唯一 Owner，重新执行完整 quick。
