# Negative paths

- Workflow state supplies no shell command, executable, flags, or environment.
- Workspace requests cannot broaden constructor write scope.
- Harness control paths are rejected as product mutations and Git commit paths.
- GitHub tokens are absent from configuration representation, receipts,
  evidence, and surfaced transport failures.
- Missing credentials and external identity drift return blocked results.
- The concrete GitHub Host does not implement merge.
- No Skill, Workflow, Capability, Executor, or Integration registry row changed.
- Customer application source remains unchanged.
- Adapter PASS, commit, PR, CI wait, Quality PASS, and Graph END do not complete
  TaskRun.
