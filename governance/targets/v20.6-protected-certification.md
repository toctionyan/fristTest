# 目标

- 目标 ID：certify-v20.6-protected-release
- 变更标识：immutable-ci-commit-required
- 执行上下文：ci
- 目标类型：certification

在不可变提交和受保护 preproduction 环境中认证 V20.6 的 PostgreSQL、双服务、真实模型与 clean artifact 链路。

## 允许范围

CI 只读取当前提交并生成 evidence 与制品。

- 允许变更路径：**
- 新增抽象记录：ci-not-applicable

## 禁止范围

不修改源码，不使用 local profile，不使用 SQLite，不用模型 stub 冒充真实模型，不使用 existing-node-modules 构建 protected artifact。

## 验收条件

- 最低质量模式：release
- 声明清单：governance/claims/v20.6-protected-certification.json
- 验收 ID：BUSINESS-PG-RUNTIME-001, FENCE-PG-RUNTIME-001, PROTECTED-SERVICE-CHAIN-001, REAL-MODEL-001, CLEAN-ARTIFACT-PREFLIGHT-001

全部 release claims 必须 VERIFIED，最终 artifact 还必须由 build_clean_release 的 protected-release 模式绑定本次 evidence。

## 基线

CI 验证当前不可变提交；本地 repair baseline 不替代 release certification。

## 修复轮次

- 最大轮次：8
- 当前轮次：1
- 失败后：CI 只输出阻断或失败证据；源码修复必须回到新的 local-change target。
