# 目标

- 目标 ID：repair-v20.8-browser-replan-evidence-closure
- 变更标识：working-tree-v20.8-browser-replan-evidence-closure
- 执行上下文：local-change
- 目标类型：repair
- 重规划来源证据：`.quality/evidence/v20.7-browser-conversation-round-04-full-02`
- 重规划失败 Gate：`configured-model-browser-conversation`

承接上一目标的 `ARCHITECTURE_REPLAN_REQUIRED` 红证据，修复“已验证的历史可见 ResultRef 没有进入 Answer Release 证据域”这一架构缺口，并让质量控制器验证重规划来源的签名、停止状态、失败 Gate 和目标身份。最终仍须由当前配置真实模型的 Chromium 强上下文旅程证明修复，不能只靠单元测试自证。

## 允许范围

- 允许变更路径：`architecture-skill/**`, `governance/**`, `scripts/**`, `services/agent-service/**`, `docs/**`, `Makefile`, `README.md`, `CHANGELOG.md`, `VERSION`
- 新增抽象记录：无

## 禁止范围

不重置、修改或伪造上一目标的停止状态和红证据；不放宽 Answer Release、CapabilityGate、目标绑定、真实模型语义 Oracle、刷新一致性或现有浏览器旅程；不以 API、固定模型、静态 JSON 或脚本直调替代真实网页证据。

## 验收条件

- 最低质量模式：integration
- 声明清单：`governance/claims/v20.8-browser-replan-evidence-closure.json`
- 验收 ID：`REPLAN-BROWSER-EVIDENCE-LINEAGE-001`

控制器必须拒绝未签名、被篡改、不是 `ARCHITECTURE_REPLAN_REQUIRED`、失败 Gate 不匹配或自引用的重规划来源。完整 Integration 必须同时通过控制器/运行时测试和 `configured-model-browser-conversation`：在“全部订单 → 退款插入 → 在路上集合 → 其中最贵”的真实网页长上下文中，已验证的最新可见单元素集合可作为 Answer Release 的正式证据，实时结果与刷新历史一致。

## 基线

先加入重规划血缘反例但尚未实现控制器血缘校验时运行 Integration 红基线；`python-test-suites` 必须因该反例失败，从而使唯一验收声明为 FAILED。上一目标的已签名红证据是本目标的问题来源，但不冒充本目标自己的 baseline。

## 修复轮次

- 最大轮次：4
- 当前轮次：3
- 失败后：只修复 repair-plan 指向的唯一 Owner；先运行血缘反例与 Answer Release 运行时回归，再执行真实模型 Chromium Gate 和完整 Integration。连续两轮无改善则再次停止并重规划。
