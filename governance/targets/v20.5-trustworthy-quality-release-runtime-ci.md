# 目标

- 目标 ID：ci-20260712-v20-5-trustworthy-quality-release-runtime
- 变更标识：workspace-v20.5.0-skill-v5.10.0-final-verification
- 执行上下文：ci

对 V20.5.0 / Skill V5.10.0 的当前源码执行不可部分复用的完整 quick 验证，并生成只能绑定该源码快照的 clean-release 授权 evidence。

## 允许范围

此 target 只验证当前工作树，不授权控制器修改源码；修复仍必须由开发 Agent 在原 local-change target 的范围内完成。

- 允许变更路径：`**`
- 新增抽象记录：`ci-not-applicable`

## 禁止范围

禁止使用 `--rerun-from`、历史 prerequisite PASS、手工编辑 summary、降低 Gate、跳过失败测试或在验证后修改待发布源码。

## 验收条件

- 最低质量模式：quick
- 当前模式全部 required Gate 必须在同一次执行中 PASS，`selected_gate_ids` 必须与 `required_gate_ids` 完全相同。
- evidence attestation、质量策略指纹、源码文件清单和源码快照必须匹配当前工作区。
- clean-release 只接受此类完整 evidence，随后仍需对白名单 staging 和最终 ZIP 解包复验。

## 基线

CI 当前态验证不使用 local baseline；完整源码快照直接记录在本次签名 evidence 中。

## 修复轮次

- 最大轮次：8
- 当前轮次：1
- 失败后：返回 local-change target 根据 repair-plan 修复；当前 CI target 不通过定向 Gate 冒充完成。
