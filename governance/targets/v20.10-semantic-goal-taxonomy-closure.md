# 目标

- 目标 ID：repair-v20.10-semantic-goal-taxonomy-closure
- 变更标识：working-tree-v20.10-semantic-goal-taxonomy-closure
- 执行上下文：local-change
- 目标类型：repair
- 重规划来源证据：`.quality/evidence/v20.9-current-turn-goal-repair-round-03-full`
- 重规划失败 Gate：`python-test-suites`

承接 v20.9 的 `ARCHITECTURE_REPLAN_REQUIRED` 红证据，修复语义 Goal Oracle、模型目标声明与 Capability `goal_completion_types` 之间的类型矛盾。事实存在性、列表、详情和状态问题必须统一声明为 `query`；政策、建议和资格问题才声明为 `consult`。不得通过让任意只读能力同时完成所有 Goal 类型来恢复绿灯。

## 允许范围

- 允许变更路径：`architecture-skill/**`, `governance/**`, `scripts/**`, `services/agent-service/**`, `docs/**`, `Makefile`, `README.md`, `CHANGELOG.md`, `VERSION`
- 新增抽象记录：无

## 禁止范围

不修改或重置 v20.9 的停止状态与签名证据；不放宽 Goal Alignment、CapabilityGate、Goal Coverage、Answer Release 或真实浏览器语义 Oracle；不把 `list_invoices` 等事实读取能力声明为通用咨询完成器；不删减强上下文轮次，不以 API、脚本或确定性模型替代真实网页证据。

## 验收条件

- 最低质量模式：integration
- 声明清单：`governance/claims/v20.10-semantic-goal-taxonomy-closure.json`
- 验收 ID：`SEMANTIC-GOAL-TAXONOMY-001`

独立语义 Oracle、模型目标声明和注册能力必须对“有没有发票”一致判定为事实查询，`list_invoices` 只能据此闭合 `query`，不能闭合政策咨询。完整 Integration 必须通过所有 22 Gate，并由配置真实模型 Chromium 完成 10+3 轮、两次刷新和新线程隔离，证明类型收紧没有破坏真实产品对话。

## 基线

在修改语义用例前运行本目标的 Integration `--baseline`；`semantic_order_detail_and_invoice` 必须稳定复现 `python-test-suites` 红灯，唯一 claim 必须为 FAILED。v20.9 的签名停止证据只作为重规划来源，不能替代本目标 baseline。

## 修复轮次

- 最大轮次：4
- 当前轮次：3
- 失败后：只修复 repair-plan 指向的唯一 Owner；先运行语义 Goal Oracle 定向回归，再运行完整 Integration。连续两轮无改善则停止并重新规划。
