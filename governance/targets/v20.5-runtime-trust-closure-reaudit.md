# 目标

- 目标 ID：change-20260712-runtime-trust-closure-reaudit
- 变更标识：working-tree-runtime-trust-closure
- 执行上下文：local-change

关闭复审发现的源码身份、protected release、Business 持久化、原子 fencing、
RAG 重试幂等和 release provenance 缺口，并验证真实 protected 服务链可运行。

## 允许范围

允许修改可信边界实现、迁移、CI、正反例、Skill/文档和交付元数据。

- 允许变更路径：architecture-skill/**, services/agent-service/**, services/business-service/**, deployment/**, scripts/**, governance/**, docs/**, release/**, .github/workflows/**, Makefile, README.md, CHANGELOG.md
- 新增抽象记录：docs/architecture/V20_5_TRUST_BOUNDARIES.md

## 禁止范围

不绕过 Draft/Grant/Attempt/Receipt，不把 verifier 降级为 candidate，不以 local
服务冒充 protected release，不删除或放宽失败反例，不复用旧 round-02 evidence。

## 验收条件

- 最低质量模式：quick
- runtime 生产源码变化必须改变 workspace fingerprint，服务运行产物变化不得改变。
- protected Business 拒绝 SQLite；PostgreSQL 多实例共享业务事实和幂等记录。
- 陈旧 checkpoint token 的物理写入次数为零。
- 同一文档 job 重试只 upsert 同一 doc_id。
- protected Agent/Business 以 preprod + PostgreSQL + strict/model verifier 通过产品 smoke。
- release 绑定唯一 evidence bundle、attestation、commit 和 workflow identity。

## 基线

从本次修复工作区建立新的独立 baseline；旧 round-02 evidence 明确作废且不复用。

## 修复轮次

- 最大轮次：8
- 当前轮次：1
- 失败后：只在新增或保留反例并完成实际修复后推进轮次；最终必须重跑完整 quick required Gate。
