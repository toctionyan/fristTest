# 目标

- 目标 ID：change-20260711-v20-4-explicit-goal-binding
- 变更标识：workspace-v20.4-goal-binding-and-real-repair-loop
- 执行上下文：local-change

将 TurnGoalPlan 从工具名猜测改成纯语义声明；让每个业务调用显式绑定单一目标并在 dispatch 前强制依赖；用删除、交换和前置失败反例验证；同时要求质量 Loop 在失败轮次后检测到真实范围内修复。

## 允许范围

- 允许变更路径：architecture-skill/**, services/agent-service/**, services/business-service/**, scripts/**, governance/**, docs/**, release/**, README.md, CHANGELOG.md, VERSION
- 新增抽象记录：无

## 禁止范围

不得改变 Business Service 的业务事实权威、不得绕过 Draft/Grant/Attempt/Receipt、不得让目标声明阶段看到或猜测业务工具、不得以删除反例或放宽断言恢复通过、不得清理开发环境数据库依赖缓存。

## 验收条件

- 最低质量模式：quick
- 同名工具不能让一个调用覆盖两个目标；缺失、未知或多个 goal_ids 在业务端口前阻断。
- depends_on 只有在前置 Step SUCCEEDED/SKIPPED 后才放行。
- 14 条独立语义 Oracle 与 84 条明确标注的 Runtime Contract 全部通过。
- adversarial counterexample Gate、标准后端测试、前端 Vitest/build 和版本一致性全部通过。
- 失败轮次后只修改当前轮次不得推进 Loop，必须有新的范围内实际修复。

## 基线

在 V20.4 版本元数据与最终报告尚未完全对齐时记录 Quick baseline，保留其真实失败作为 repair-plan 输入；运行时代码的更早红灯由目标绑定反例测试输出保留。

## 修复轮次

- 最大轮次：8
- 当前轮次：1
- 失败后：先读取 repair-plan 并保留反例，再修改真正 Owner；确认 repair fingerprint 改变后推进一轮并定向回归，最终运行完整 Quick。
