# Negative paths

The repair changes only immutable file publication. It adds no command execution,
Host authority, write grant, Workflow route, TaskRun transition, Quality verdict,
merge behavior or completion transition. A losing publisher removes only its own
temporary file.

