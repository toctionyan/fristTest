# Negative paths

- Local-only installation does not require or invent GitHub credentials.
- Configured GitHub never stores a token and refuses to start when its named variable is absent.
- Full-dev/repair mutation remains blocked because the built-in factory injects no `WriteAuthorityGuard`.
- No Human Gate decision adapter, completion controller, release writer, merge adapter, or automatic merge path is added.
- Runtime output cannot be written into the immutable Starter package.
- Initialization and Host selection have `authority_effect=false`.
