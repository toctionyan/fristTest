# Negative paths

PASS.

- `configuration_grants_write_authority` remains false.
- Missing project governance blocks at the first mutating dispatch; read-only Host operations still assemble.
- `SELECT`, `CONFIRM`, capability binding, and bootstrap presence are never accepted as ChangePermit substitutes.
- Generic write authority always rejects `code_review.pull_request.merge`.
- Human decisions carry `authority_effect=false` and cannot change completion or merge authority.
- Graph `END` still projects TaskRun `VALIDATING`.
- Services, web, contracts, product dependencies, Judge inputs, merge code, release code, and production code are unchanged.
