# 目标

- 目标 ID：repair-v20.7-terminal-transaction-state
- 变更标识：working-tree-v20.7-terminal-transaction-state
- 执行上下文：local-change
- 目标类型：repair

修复真实 Chromium 人工验收发现的终态事务语义矛盾：事务已经提交并有 Receipt 时，卡片主标题不能继续显示“待办理事务”；同一状态合同必须由组件反例和真实浏览器旅程共同保护。

## 允许范围

- 允许变更路径：`services/agent-service/frontend/**`, `services/agent-service/tests/architecture/**`, `governance/claims/v20.7-terminal-transaction-state.json`, `governance/targets/v20.7-terminal-transaction-state.md`
- 新增抽象记录：无

## 禁止范围

不改变事务 Lifecycle、授权、提交或 Receipt 事实；不以隐藏卡片、删除终态展示或仅修改截图绕过状态语义验证。

## 验收条件

- 最低质量模式：quick
- 声明清单：`governance/claims/v20.7-terminal-transaction-state.json`
- 验收 ID：`TERMINAL-TRANSACTION-STATE-001`

Vitest 必须验证终态标题不再保留 pending 语义；真实 Chromium 必须完成 Draft、输入、授权、提交和 Receipt，并明确返回 `terminalTitleAccurate=true`。

## 基线

冻结组件反例和 Chromium 断言后，以截图中实际存在的“办理结果 / 待办理事务 / 已提交”矛盾实现记录红基线。

## 修复轮次

- 最大轮次：4
- 当前轮次：1
- 失败后：只修改事务卡片唯一标题派生规则，定向通过后重跑完整 Quick Judge。
