# 目标

- 目标 ID：certify-v20.6-local-candidate
- 变更标识：working-tree-v20.6-local-candidate
- 执行上下文：local-change
- 目标类型：certification

在当前源码快照上统一认证 V20.6 的 claim 驱动质量 Loop、证据选择器、Business 方言边界、异步 fencing fail-closed、候选/受保护发布边界、Skill 对齐与环境阻断传播。该目标只授权本地 candidate，不冒充 protected release。

## 允许范围

本目标只生成治理证据、候选发布元数据和验证报告，不修改业务实现。

- 允许变更路径：governance/**, release/**, docs/**
- 新增抽象记录：无

## 禁止范围

不把 quick 证据升级为 protected release；不跳过完整 required Gate；不复用源码快照不一致的旧 evidence；不把环境阻断写成 VERIFIED 或代码 FAIL。

## 验收条件

- 最低质量模式：quick
- 声明清单：governance/claims/v20.6-local-candidate-certification.json
- 验收 ID：QL-CLAIM-001, QL-CLAIM-002, BUSINESS-DIALECT-001, FENCE-ASYNC-001, RELEASE-BOUNDARY-001, SKILL-LOOP-ALIGNMENT-001, QL-ENV-BLOCK-001

所有本地 quick claims 必须在同一源码快照、同一完整 quick 运行中 VERIFIED。protected PostgreSQL、双服务、真实模型和不可变 CI provenance 由独立 `v20.6-protected-certification` 目标认证；环境缺失时必须保持 `BLOCKED_BY_ENVIRONMENT`。

## 基线

先以当前 target、claim 和 policy 建立 static baseline；候选认证不得引用其他目标的历史 PASS 关闭本目标。

## 修复轮次

- 最大轮次：8
- 当前轮次：1
- 失败后：保留失败证据并返回对应 claim 的唯一 Owner；只有完整 quick 同轮通过才可生成 candidate artifact。
