# 目标

- 目标 ID：repair-v20.7-product-runtime-gates
- 变更标识：working-tree-v20.7-product-runtime-gates
- 执行上下文：local-change
- 目标类型：repair

把已经收敛的内部 Runtime 不变量提升为用户可操作的产品闭环：隔离双服务完整生命周期必须能从公开 API 跑到 Receipt，真实 Chromium 必须覆盖桌面与移动关键旅程，release 必须调用真实模型穿过完整 Graph 而不是停留在目标声明原型。

## 允许范围

- 允许变更路径：`architecture-skill/**`, `governance/**`, `scripts/**`, `services/agent-service/**`, `services/business-service/**`, `docs/**`, `release/**`, `.github/**`, `Makefile`, `README.md`, `CHANGELOG.md`, `VERSION`
- 新增抽象记录：`docs/architecture/NEW_ABSTRACTION_PRODUCT_RUNTIME_GATES.md`

## 禁止范围

不降低认证、事务授权、CapabilityGate、PostgreSQL fencing、真实模型 verifier、coverage 或 clean-release 要求；不以 jsdom、内部函数调用、mock fetch 或仅目标声明冒充真实浏览器和完整生命周期。

## 验收条件

- 最低质量模式：quick
- 声明清单：`governance/claims/v20.7-product-runtime-gates.json`
- 验收 ID：`FULL-LIFECYCLE-CANARY-001`, `PRODUCT-BROWSER-JOURNEY-001`

Quick 必须在隔离临时数据上启动真实 Agent/Business HTTP 边界与确定性模型，完整执行登录、订单、对话、事务与 Receipt；Chromium 必须分别以桌面和 390px 移动视口执行产品旅程。真实模型完整 Graph 作为独立 release Gate 和本轮完成后的 protected certification 运行，不得由 Quick 替代。

## 基线

在 Gate 合同已冻结但 canary、Playwright 依赖与浏览器脚本尚不存在时执行 Quick 红基线，证明 Loop 会阻断缺少真实产品路径的候选。

## 修复轮次

- 最大轮次：8
- 当前轮次：3
- 失败后：只修复失败 Gate 的唯一 Owner；先定向重跑完整生命周期与真实浏览器，再执行完整 Quick Judge。
