# 目标

- 目标 ID：repair-v20.7-systemic-operational-closure
- 变更标识：working-tree-v20.7-systemic-operational-closure
- 执行上下文：local-change
- 目标类型：repair

把项目从“已声明 Gate 变绿”提升为可运行的系统闭环：协议安全的长上下文、跨轮证据满足、真实多线程拓扑、模块运行闭包、前端产品状态，以及能由 Mutation 与脱敏 Replay 自证有效的质量 Loop。

## 允许范围

允许修改本地架构 Skill、治理目录、质量控制器与 Gate、Agent 生命周期/上下文/观测模块、电商模块公开接口、对话 runner、前端产品状态及其测试。

- 允许变更路径：`architecture-skill/**`, `governance/**`, `scripts/**`, `services/agent-service/**`, `services/business-service/**`, `docs/**`, `Makefile`, `README.md`, `CHANGELOG.md`, `VERSION`
- 新增抽象记录：`docs/architecture/NEW_ABSTRACTION_SYSTEMIC_OPERATIONAL_CLOSURE.md`

## 禁止范围

不降低 protected profile、事务授权、PostgreSQL fencing、语义 verifier、coverage 或 clean-release 要求；不通过增加消息数量、放宽 Oracle、跳过真实拓扑或固定单个 happy path 伪造闭环。

## 验收条件

- 最低质量模式：quick
- 声明清单：`governance/claims/v20.7-systemic-operational-closure.json`
- 验收 ID：`CONTEXT-PROTOCOL-ATOMICITY-001`, `GOAL-EVIDENCE-SATISFACTION-001`, `MODULE-PUBLIC-RUNTIME-001`, `SCENARIO-TOPOLOGY-SEQUENCE-001`, `FRONTEND-PRODUCT-STATE-001`, `LOOP-REQUIREMENT-COMPLETENESS-001`, `LOOP-MUTATION-ADEQUACY-001`, `RUNTIME-FAILURE-REPLAY-001`

每条 claim 必须由新增系统反例在 baseline 中失败；最终必须经过系统反例 Gate、全量 Python、完整前端 App 测试和 coverage Gate。真实模型与双服务作为修复完成后的独立 certification 执行，不能由 Quick 证据代替。

## 基线

在加入协议切断、历史 Evidence 被误判、wildcard 私有符号丢失、单线程伪拓扑、订单选择回退、非累积 requirement profile、等价错误 Mutation 和缺少 Replay 的反例后，使用本 target 执行 Quick 红基线。

## 修复轮次

- 最大轮次：8
- 当前轮次：2
- 失败后：只修改失败 invariant 的唯一 Owner；先重跑 `systemic-operational-counterexamples` 依赖闭包，再执行完整 Quick。连续两轮无改善时停止并重新规划。
