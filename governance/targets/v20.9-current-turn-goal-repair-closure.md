# 目标

- 目标 ID：repair-v20.9-current-turn-goal-repair-closure
- 变更标识：working-tree-v20.9-current-turn-goal-repair-closure
- 执行上下文：local-change
- 目标类型：repair
- 重规划来源证据：`.quality/evidence/v20.8-browser-replan-evidence-round-03-full`
- 重规划失败 Gate：`configured-model-browser-conversation`

修复真实模型在目标声明重试中把历史或幻觉文本当作当前用户原文、而 Runtime 只返回错误码导致循环耗尽的问题。权威当前 user text 必须进入每次模型规划提示和目标声明错误回执；模型仍须自行重新声明语义目标，Runtime 不自动生成 goal、evidence span 或工具调用。

## 允许范围

- 允许变更路径：`architecture-skill/**`, `governance/**`, `scripts/**`, `services/agent-service/**`, `docs/**`, `Makefile`, `README.md`, `CHANGELOG.md`, `VERSION`
- 新增抽象记录：无

## 禁止范围

不修改或重置 v20.8 停止状态；不把历史商品名、订单号或后续轮次写入当前 user text；不自动补目标或放宽字面 span、Goal Alignment、Workflow、CapabilityGate、Answer Release 与真实浏览器语义 Oracle；不删减强上下文旅程。

## 验收条件

- 最低质量模式：integration
- 声明清单：`governance/claims/v20.9-current-turn-goal-repair-closure.json`
- 验收 ID：`CURRENT-TURN-GOAL-REPAIR-001`

错误目标声明必须返回精确、只读的 `current_user_input` 与“evidence_span 必须来自该文本”的修正合同；下一次模型调用必须清楚标识同一权威原文。完整 Integration 必须继续通过所有 22 Gate，并由配置真实模型 Chromium 完成 10+3 轮、两次刷新和新线程隔离。

## 基线

先加入“错误回执必须包含权威当前原文”的反例，在 Runtime 尚未实现时记录 Integration 红 baseline；`python-test-suites` 与唯一 claim 必须为 FAILED。v20.8 的签名停止证据只作为架构重规划来源，不能替代本目标 baseline。

## 修复轮次

- 最大轮次：4
- 当前轮次：3
- 失败后：只修复 repair-plan 的唯一 Owner；先验证目标声明错误回执和真实网页四轮，再运行完整 Integration。连续两轮无改善则停止。
